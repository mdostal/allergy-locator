# Story assets

Drop the images referenced by the story docs here. Suggested names (match the `[drop pic here]`
slots):

- `2013-baseline-sheet.*` — the handwritten "wild welts" battery (the everything-lights-up baseline)
- `2021-grids.*` — the green/blue/yellow color-coded skin-test panels
- `welts-to-dots.*` — the progression photos: wild welts → quiet dots

## ⚠ Before adding any image
This is a **public** repo. **Crop or black out all PHI** first — name, DOB, driver's-license
number, patient ID, insurance, provider name, address, phone. Publish the *reactions and
numbers*, never the identifiers. When in doubt, leave it out.
