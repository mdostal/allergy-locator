{
  "name": "@allergy-locator/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing allergy-locator's real scoring engine (not a reimplementation) as agent-callable tools.",
  "type": "module",
  "bin": {
    "allergy-locator-mcp": "./bin/run.mjs"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.0",
    "typescript": "^5",
    "vitest": "^3"
  }
}
