# Optional "live conditions" toggle only. The core map needs zero keys.
# This key is read ONLY by the serverless function at /api/pollen — never
# shipped to the browser, never committed with a real value.
GOOGLE_POLLEN_API_KEY=
